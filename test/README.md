# Simple Shell Files Test

To compile and test the programs in the parent directory; cd into this (`test`) directory and run the command:

- `gcc -Wall -Werror -Wextra -pedantic -std=gnu89 ../*.c -o hsh`
